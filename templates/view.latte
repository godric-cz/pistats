<!DOCTYPE html>
<html>
<head>
    <style>
    body { font-family: Verdana, sans-serif; font-size: 13px; }
    table { border-collapse: collapse; }
    td, th { border: solid 1px #999; padding: 0.3em 0.5em; }
    tr:nth-child(odd) { background-color: #eee; }
    th { background-color: #ddf; }
    tr:hover td { background-color: #ddd; }
    </style>
</head>
<body>

<table>
    {foreach $rows as $row}
    {first}
    <tr>
        <th n:foreach="$row as $key => $value">{$key}</th>
    </tr>
    {/first}
    <tr>
        <td n:foreach="$row as $value">{$value}</td>
    </tr>
    {/foreach}
</table>

</body>
</html>
